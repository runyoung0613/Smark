## Smark 环境变量示例
#
# 复制本文件为 `.env`（不要提交 `.env`）：
#   - Windows PowerShell:
#       Copy-Item .env.example .env
#
# 然后把下面两个值替换为你自己的 Supabase 项目配置：
#   1) EXPO_PUBLIC_SUPABASE_URL
#      形如：https://<project-ref>.supabase.co
#   2) EXPO_PUBLIC_SUPABASE_ANON_KEY
#      Supabase 项目的 anon public key
#
# 配好后重启开发服务（建议清缓存）：
#   npx expo start -c

EXPO_PUBLIC_SUPABASE_URL="https://<your-project-ref>.supabase.co"
EXPO_PUBLIC_SUPABASE_ANON_KEY="<your-anon-public-key>"

# 可选：用户于「个人中心」填写 Perses Key 时，App 将 POST 到此地址并携带 Authorization: Bearer <Key>
#（直连网关由开发者部署或与上游约定；未配置时 Perses 可走登录后的云端 perses_proxy）
# EXPO_PUBLIC_PERSES_HTTP_URL="https://your-gateway.example.com/perses"

# 可选：直连阿里云百炼 OpenAI 兼容接口时，覆盖默认模型（默认 qwen-turbo）
# EXPO_PUBLIC_PERSES_DASHSCOPE_MODEL="qwen-plus"